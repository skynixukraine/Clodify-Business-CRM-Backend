<div class="slider-container">
  <div class="slider-control left inactive"></div>
  <div class="slider-control right"></div>
  <ul class="slider-pagi"></ul>
  <div class="slider">
    {{#each model as |slider|}}
      <div class="slide">
        <div class="slide-content">
          <div class="slide-img">
            <img src="{{slider.img}}" alt="" class="slide-pic">
          </div>
          <div class="slide-text">
            <div class="text-wrap">
              <h2 class="text-heading">Why <span>Skynix</span></h2>
              <p class="text-desc">{{slider.description}}</p>
              <div class="text-subtitle">{{slider.title}}</div>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>